# AGENTS.md

Project-specific rules live here.

- Read `README.md` first before planning or implementation.
- Follow the global `%USERPROFILE%/AGENTS.md` rules and workflows.
- Keep implementation pragmatic: YAGNI, KISS, DRY.
- Prefer updating existing files over creating parallel "enhanced" variants.
- Keep code files small when practical; modularize when a file starts getting too large or mixes concerns.
- After implementation, sync plan status and update project docs in `./docs/`.
- Use the relevant skills for the task instead of treating the workflow as optional.

If `CLAUDE.md` is present, it should defer to this file.
